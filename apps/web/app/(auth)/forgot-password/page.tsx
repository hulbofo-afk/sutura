"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { Button } from "@/components/ui/button";
import { TextField } from "@/components/ui/field";
import { apiFetch } from "@/lib/api";
export default function ForgotPassword(){const[done,setDone]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");try{await apiFetch("auth/forgot-password",{method:"POST",body:JSON.stringify({email:String(new FormData(e.currentTarget).get("email"))})});setDone(true)}catch(reason){setError(reason instanceof Error?reason.message:"Envoi impossible.")}}return <main className="grid min-h-screen place-items-center bg-canvas p-5"><form onSubmit={submit} className="w-full max-w-md space-y-6 rounded-[24px] border border-line bg-white p-8 shadow-lift"><p className="t-eyebrow text-framboise">Accès à ton atelier</p><h1 className="display-font text-4xl font-semibold text-prune">Mot de passe oublié.</h1><p className="text-sm leading-6 text-prune/60">Entre ton email. Si le compte existe, tu recevras un lien valable une heure.</p>{done?<p role="status" className="rounded-[14px] bg-success-clair p-4 text-sm font-semibold text-success">Vérifie ta boîte de réception.</p>:<><TextField label="Adresse email" name="email" type="email" required autoComplete="email"/>{error&&<p role="alert" className="text-error">{error}</p>}<Button className="w-full">Envoyer le lien</Button></>}<Link href="/login" className="block text-center text-sm font-bold text-framboise">Retour à la connexion</Link></form></main>}
